import { DashboardLayout } from "./DashboardLayout";
import { StatCard, StatsGrid } from "./StatCard";
import DetailPanel from "./DetailPanel";
import { SectionCard, Tabs, Toolbar } from "./SectionCard";
import AdminDashboardNew from "./AdminDashboard";
import ModeratorDashboard from "./ModeratorDashboard";
import UserDashboard from "./UserDashboard";

export {
  // Layout
  DashboardLayout,
  // Stats
  StatCard,
  StatsGrid,
  // Data Display
  DetailPanel,
  // Sections
  SectionCard,
  Tabs,
  Toolbar,
  // Dashboard Pages
  AdminDashboardNew,
  ModeratorDashboard,
  UserDashboard,
};
