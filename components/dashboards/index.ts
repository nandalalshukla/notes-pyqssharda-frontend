import { DashboardLayout } from "./DashboardLayout";
import { StatCard, StatsGrid } from "./StatCard";
import { DataTable } from "./DataTable";
import DetailPanel from "./DetailPanel";
import { FilterGroup, SidebarFilter, ActiveFilters } from "./Filters";
import { SectionCard, Tabs, Toolbar } from "./SectionCard";
import AdminDashboardNew from "./AdminDashboard";
import ModeratorDashboard from "./ModeratorDashboard";
import UserDashboard from "./UserDashboard";

export {
  // Layout
  DashboardLayout,
  // Stats
  StatCard,
  StatsGrid,
  // Data Display
  DataTable,
  DetailPanel,
  // Filtering
  FilterGroup,
  SidebarFilter,
  ActiveFilters,
  // Sections
  SectionCard,
  Tabs,
  Toolbar,
  // Dashboard Pages
  AdminDashboardNew,
  ModeratorDashboard,
  UserDashboard,
};
